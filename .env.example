# ACUR Embedding Service Configuration
# Copy this file to .env and modify as needed

# Service host (default: 0.0.0.0 - accepts connections from any IP)
# Use 127.0.0.1 to only accept local connections
EMBEDDING_SERVICE_HOST=0.0.0.0

# Service port (default: 8000)
# Change this if port 8000 is already in use
EMBEDDING_SERVICE_PORT=8000

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Modify the values in .env
# 3. Run the service:
#    - Linux/Mac: ./start.sh
#    - Windows: start.bat
#    - Direct: python embedding_service.py
#
# The service will automatically read these environment variables
